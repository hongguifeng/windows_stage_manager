# Windows 11 台前调度式窗口管理器需求说明

文档版本：1.5
文档状态：可用于 MVP 评审与验收  
适用平台：Windows 11，交互用户会话

## 1. 文档目的

本文档定义产品目标、边界、可观察行为和验收标准。它不限定必须使用某一种编程语言或求解算法，也不把“绝对不发生任何瞬间遮挡”作为可实现承诺。

产品应提供类似 iPadOS 台前调度的自由窗口体验：新活动窗口可配置为在前台切换时居中，已经活动的窗口仍可由用户自由移动；窗口可以重叠，但在管理器能够控制的范围内，每个受管理窗口优先保留两个独立的可见交互区域，确实无解时至少保留一个。

## 2. 核心决策

### 2.1 可见性保证的时点

窗口位置变化通过 Windows 事件异步通知，管理器无法阻止系统在事件到达前出现短暂遮挡。因此保证定义为：

> 每个位置/尺寸事件批次处理完成并进入稳定状态后，所有受管理窗口都满足可见性约束。

拖动期间允许出现短暂的中间状态，但管理器应在下一次合并处理后的布局更新中恢复约束。

### 2.2 可见性的可验收定义

每个受管理窗口首选至少有两个不同边缘的交互区满足以下条件；若两边缘目标无解，允许明确降级为至少一个：

1. 交互区位于窗口可见框内，并贴靠左、右、上、下四条边之一。
2. 交互区长度不小于 `MinExposedEdge`，默认值为 48 DIP。
3. 交互区深度不小于 `MinExposedDepth`，默认值为 24 DIP。
4. 交互区未被更高 Z-order 的阻挡窗口覆盖。
5. 对于矩形窗口 MVP，交互区的未遮挡部分必须包含一个满足最小尺寸的矩形；该矩形的代表点经 `WindowFromPoint` 命中后，调用 `GetAncestor(GA_ROOT)` 应归属该窗口的根窗口。

上述规则是“可见且可命中”的几何代理，不声称可以理解每个应用自定义的内容区域。透明、非矩形、无法命中的窗口默认不纳入 MVP 管理。

### 2.3 无解时的策略

管理器必须先穷尽当前策略允许的纯位置方案；只有两边缘和单边缘位置求解都明确返回 `Unsatisfiable` 时，才允许尝试调整一个非活动受管理窗口的 Z-order。候选排序的第一目标是保持尽可能长的上层窗口前缀完全不变：先尝试最深层的局部插入边界，无解时才逐层向活动窗口方向扩大变化区间；同一插入边界内再按当前 Z-order 从最底层目标向上枚举。目标窗口只提升到当前候选边界的下一层，不默认提升到活动窗口正下方。较深层局部重排能形成完整合法布局时，不得触及更上层边界。活动窗口的 Z-order 不得改变；其位置在前台激活事务中固定为本显示器工作区中心，在拖动事务中固定为用户最终位置。其他窗口不得越过活动窗口，topmost/owned/系统或分类不确定窗口不得成为重排目标。

激活居中、位置修复与安全 Z-order fallback 的完整组合无解时，管理器必须返回 `Unsatisfiable`，停止本批次的自动调整且不应用部分计划。拖动事务必须保留用户正在操作的窗口位置；不得把窗口推出屏幕。

无解原因应记录到日志，并在托盘状态中提供可诊断信息。下一次窗口拓扑或尺寸变化时可以重新尝试。

## 3. 术语

| 术语 | 定义 |
| --- | --- |
| 受管理窗口 | 通过分类规则进入布局求解器、允许被管理器移动的普通顶级窗口 |
| 活动窗口 | 当前前台窗口；前台切换事务中采用工作区居中位置，拖动事务中采用用户最终位置，并拥有最高布局优先级 |
| 阻挡窗口 | 位于目标窗口上方、其可见框可能遮挡目标交互区的窗口。阻挡窗口可以受管理，也可以不可移动 |
| 工作区 | 目标显示器的 `rcWork`，通常不包含任务栏占用区域 |
| 交互区 | 用于验收可见性和命中性的窗口边缘矩形 |
| 事件批次 | 在一个短时间窗口内合并的窗口事件，批次结束后执行一次求解和验证 |
| 拖动事务 | 从 `EVENT_SYSTEM_MOVESIZESTART` 到 `EVENT_SYSTEM_MOVESIZEEND` 的一次用户移动/调整大小过程 |
| 稳定状态 | 本批次无待处理移动、所有窗口快照已刷新、硬约束已经满足或已明确无解 |

## 4. 产品范围

### 4.1 MVP 范围

MVP 必须支持：

1. Windows 11 的单个交互用户会话。
2. 当前虚拟桌面上的单个显示器布局。活动窗口跨到其他显示器时，当前事务暂停并重新识别目标显示器。
3. 1 至 20 个普通、可见、非最小化、非最大化、非全屏、非置顶的矩形顶级应用窗口；单窗口场景仍执行激活居中。
4. 普通窗口由非活动切换为活动时居中；用户正常拖动或调整已活动窗口时，保留用户最终位置。
5. 窗口可以重叠，管理器不得主动采用平铺网格；Z-order 只允许按 2.3 节作为位置无解后的受控回退。
6. 对受管理窗口执行自动移动、事件回流抑制、布局稳定性验证和无解处理。

窗口分类不确定时应选择“不管理”，优先保证系统稳定性。

### 4.2 后续范围

后续版本可以增加多显示器协同、动画、白名单/黑名单、窗口锁定、独立高级配置界面、更精确的透明/区域窗口处理和全局优化。

### 4.3 明确不属于 MVP

以下对象不承诺被管理或被移动：Windows 服务进程、Session 0、UAC 安全桌面、锁屏、远程桌面安全界面、独占全屏表面、Shell 系统窗口、菜单、工具提示、通知、模态系统对话框、始终置顶工具窗口、最小化/最大化/无边框全屏窗口。

这些对象仍可能遮挡受管理窗口。若它们使布局无解，按无解策略处理，而不是与其争夺位置。

## 5. 功能需求

### FR-01 运行方式

管理器必须作为每个交互用户会话中的后台应用运行，至少包含托盘入口、启用/暂停开关和紧急停用入口。不得假设 Windows 服务可以直接管理用户桌面。

### FR-02 窗口发现和身份

管理器必须能够发现当前桌面的顶级窗口，读取窗口句柄、进程 ID、所有者、样式、可见性、窗口框、显示器、DPI 和当前 Z-order。

HWND 不能单独作为长期身份。窗口销毁后重新出现的同值 HWND 必须被识别为新实例，旧状态不得复用。

### FR-03 窗口分类

默认候选窗口必须同时满足：

- 是根顶级窗口，不是 `WS_CHILD` 或子控件。
- 可见且未最小化。
- 不是 `WS_EX_TOOLWINDOW`、`WS_EX_NOACTIVATE` 或 `WS_EX_TOPMOST`。
- 不是 cloaked、Shell 窗口、临时菜单/提示/通知或已知系统 UI。
- 当前窗口尺寸足以容纳配置的最小交互区。

分类失败、窗口句柄失效或查询权限不足时，窗口进入“不管理”状态。分类结果变化必须触发重新枚举。

### FR-04 生命周期跟踪

管理器必须处理窗口显示、隐藏、移动、调整大小、最小化、最大化、销毁和前台变化。收到事件后不得直接相信旧快照；求解前必须重新读取参与窗口的状态。

### FR-05 拖动事务

1. 以 `MOVESIZESTART` 进入拖动事务，事件中的 HWND 作为活动窗口。
2. 事务期间合并重复位置事件，避免每个像素启动完整求解。
3. 拖动事务中的活动窗口不由求解器移动；活动窗口不可管理、最大化或全屏时，事务进入暂停/无解状态。
4. `MOVESIZEEND` 后执行一次最终重算和验证。
5. 活动窗口变化、窗口销毁或用户停用管理器时，事务必须安全结束。
6. 启用 `CenterActivatedWindow` 时，普通窗口从非活动切换为活动且未同时进入拖动事务，必须把其视觉框一次性平移到当前显示器 `rcWork` 中心；关闭时不得移动活动窗口，但仍须检查并修复被其遮挡的窗口。相同 HWND 的重复前台事件不得再次居中。

### FR-06 可见性约束

对每个受管理窗口，求解器必须基于当前 Z-order 计算其交互区被高层阻挡窗口覆盖的情况。窗口重叠本身不是错误，只有交互区违反约束才构成违规。

默认阈值如下，均以 DIP 表示并按目标显示器 DPI 转换：

```text
MinExposedEdge   = 48 DIP
MinExposedDepth  = 24 DIP
PreferredExposedEdges = 2
MinimumExposedEdges = 1
RepairTargetEdge = 64 DIP
MinOnscreenWidth = 100 DIP
MinOnscreenHeight = 100 DIP
```

窗口应优先保留两个不同边缘、长度均达到 `RepairTargetEdge` 的独立交互区。多边缘目标计算时必须从水平边缘排除左右角部、从垂直边缘排除上下角部，禁止把同一条外露窄带在两个角上的交集重复计作两条边缘；两边缘求解无解时，允许降级为至少一个。窗口宽度或高度不足以满足阈值时，默认不纳入管理。

### FR-07 自动布局

当活动窗口导致其他受管理窗口违规时，管理器应：

1. 若本批由非活动到活动的前台切换触发，居中开关开启时以活动窗口的工作区居中位置作为本批固定位置，关闭时以原位置作为固定位置；若本批由已活动窗口拖动触发，以用户最终位置作为本批固定位置。
2. 只移动必要的受管理窗口。
3. 被遮挡窗口的合法候选首先最小化其窗口中心到工作区中心的 Manhattan 距离，不设置左上、右下或单一边缘的固定方向优先级。
4. 中心距离相同时，再依次减少移动窗口数量、本次移动距离、距稳定布局距离、边界偏离和方向切换，并使用窗口身份与坐标作确定性平局键。
5. 继续处理被新位置影响的低层窗口。
6. 不改变窗口大小、激活状态或所有者关系；普通位置候选不得改变 Z-order。
7. 普通位置方案全部无解后，可按 2.3 节尝试单个窗口的底层优先 Z-order fallback，并对新的顺序重新运行完整位置求解。

候选位置必须先满足所有硬约束，再比较中心距离及其他软代价。没有完整合法方案时必须返回无解；激活居中移动必须计入每批最大移动数，并与被遮挡窗口修复和 Z-order fallback 一起原子规划。

### FR-08 稳定性

管理器必须具备滞后和事务记忆：达到首选边缘数量时不得移动；只达到最低边缘数量时应先尝试恢复首选数量，首选目标无解才保持最低目标；同一事务中不得在两个位置之间来回切换。

每个事务和事件批次必须有最大求解状态数、最大移动次数和最大处理时间。达到上限后进入无解/暂停状态，而不是继续尝试。

### FR-09 移动应用和验证

移动窗口时必须保持其尺寸、激活状态和 Z-order。执行 Z-order fallback 时必须使用 `SWP_NOMOVE | SWP_NOSIZE | SWP_NOACTIVATE | SWP_NOOWNERZORDER`，并验证活动窗口仍位于目标上方、目标位于计划层级、位置/尺寸/显示器/样式/topmost 均未改变。调用 Windows API 后必须重新读取实际状态；若应用拒绝、自动改回或窗口已经销毁，立即重新建快照并停止本批计划。

### FR-10 失败安全

事件钩子丢失、查询失败、API 调用失败、队列溢出或求解超时都必须触发重新枚举。无法确认状态时不得继续批量移动窗口。用户可以从托盘一键暂停管理器。

### FR-11 托盘参数管理

所有 MVP 运行参数必须能从中文托盘右键菜单直接选择，不要求用户手动修改配置文件。菜单必须显示当前值并勾选当前预设；连续数值参数除常用预设外必须提供“自定义…”输入入口，校验明确的最小值和最大值，并把合法整数作为当前值显示。选择新值后必须持久化，并在不退出托盘进程的情况下应用。运行参数变化时，管理器必须先取消当前移动事务并停止内部协调器，完成参数依赖归一化后再以新配置重建，避免 UI 线程与协调器线程并发读写配置。

托盘至少提供 DryRun、新激活窗口居中开关、暴露边缘长度/深度、首选/最低边缘数、修复目标长度、最小屏上宽高、事件合并与 reconciliation 间隔、移动/求解/窗口数量上限和连续失败阈值。必须保持 `MinimumExposedEdges <= PreferredExposedEdges` 与 `MinExposedEdge <= RepairTargetEdge`；预设或自定义值导致冲突时自动调整其依赖项。重建失败时进入 `ApiError`，不得假装新配置已正常运行。

## 6. 非功能需求

### NFR-01 响应时间

在 20 个 MVP 窗口、矩形可见性模型下，事件批次从收敛到应用的 P95 时间目标为 16 ms，P99 超时必须有指标记录。正确性和稳定性优先于强行达到 60 FPS。

### NFR-02 确定性

相同窗口快照、相同活动窗口和相同配置必须产生相同的候选排序和布局结果。所有代价比较必须有稳定的平局规则。

### NFR-03 可诊断性

至少记录事件批次、活动窗口、是否采用激活居中、违规窗口、候选拒绝原因、实际移动结果、无解原因和耗时。日志不得记录窗口内容或敏感文本。

### NFR-04 权限和会话

程序只操作当前交互用户桌面。遇到跨完整性级别、受保护窗口或安全桌面导致的查询/移动失败时，必须降级为不管理或无解，不得反复重试。

## 7. 验收标准

1. 两个普通窗口部分重叠并拖动活动窗口，非活动窗口在可行时至少有两个不同边缘的合规交互区；两边缘无解时至少有一个，并记录降级。
2. 活动窗口从四个方向覆盖目标窗口时，四方向候选均能正确验证；满足硬约束后优先选择更接近工作区中心的候选，不能使用固定方向偏好。
3. 三个以上窗口形成 A -> B -> C 的遮挡链时，求解器能处理链式影响，且不会重复来回移动。
4. 所有候选都被其他窗口或工作区边界拒绝时，系统进入无解状态，不把窗口推出屏幕。
5. 最大化、全屏、置顶、菜单、通知和未分类窗口不会被自动移动；它们造成遮挡时有可诊断的无解记录。
6. 管理器自身的 `SetWindowPos` 事件不会重新触发无限布局循环。
7. 窗口销毁后立即创建同句柄窗口时，旧位置和旧事务状态不会泄漏到新窗口。
8. 100%、150%、200% DPI 下，阈值按 DIP 缩放，窗口框和工作区使用同一坐标系。
9. 快速拖动、调整大小、Windows Snap 和前台切换不会导致持续抖动或大幅无关跳跃。
10. 用户暂停管理器后，后续事件不再触发自动移动。
11. 用户可从中文托盘修改全部运行参数；连续数值字段既可选预设也可输入范围内的自定义整数。选择后配置立即落盘，内部管理器使用新参数重建，托盘进程保持运行，DryRun 切换无需编辑文件或重启。
12. 居中开关启用时，普通窗口从非活动切换为活动，其视觉框中心与当前显示器工作区中心重合；关闭时活动窗口保持原位。相同窗口已活动后的重复前台事件和手动拖动不会再次触发居中。
13. 激活居中导致其他窗口违规时，活动窗口居中与非活动窗口修复必须组成一个完整合法计划；无解或超出移动上限时不得只应用其中一部分。
14. 一条外露窄带只能贡献对应的一条独立边缘；只有横向和纵向均形成扣除角部后仍合格的边段时，才能满足默认两边缘目标。

## 8. 版本路线

### MVP-1：单窗口对单窗口

完成发现、保守分类、事件队列、DPI 坐标、矩形遮挡和候选硬约束验证。先支持两个窗口并验证无解策略。

### MVP-2：多窗口稳定求解

增加多阻挡窗口、链式影响、事务记忆、滞后、状态哈希和失败回滚/暂停。

### V2：平台能力扩展

增加多显示器、虚拟桌面感知、用户配置、白名单/黑名单、锁定窗口和可选动画。
