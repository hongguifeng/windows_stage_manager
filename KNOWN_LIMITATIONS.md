# 已知限制

- 仅支持 Windows 11 桌面会话；安全桌面、UAC 提权窗口和更高完整性级别窗口可能无法查询或移动。
- 自动求解限制在活动窗口所在显示器，同一批次不会把窗口迁移到另一显示器。
- 最大化、全屏、置顶、工具、菜单、提示、系统 UI、owned window、最小化和非当前虚拟桌面窗口不会作为移动目标。
- 可见性使用 DWM 矩形与矩形区域模型；透明、非矩形窗口和应用自绘点击区域可能与视觉结果不同。
- Windows Snap、应用主动回弹位置或窗口在应用期间销毁会中止批次并进入重建/降级，不保证继续完成原计划。
- 求解器有窗口数、候选数、状态数、移动次数、区域复杂度和耗时上限；超过上限会返回无解/超时，不降低硬约束。
- Z-order fallback 只处理受管理的普通非置顶窗口，且每批只提升一个窗口到活动窗口正下方；置顶、owned、系统或分类不确定窗口仍只作为阻挡，不会被重排。
- 当前不包含动画、跨显示器全局优化、安装程序或自动更新器。发布物是便携 ZIP。
- `scenario_runner` 的耗时用于同一机器上的回归比较，不应把 Debug 数值当作发布性能结论。

报告问题时请附上版本、复现步骤、显示器/DPI 布局、相关 `batch_complete` 日志和可重复的随机种子；日志可能包含 HWND、进程 ID、窗口类名和矩形位置，请在分享前审查。
