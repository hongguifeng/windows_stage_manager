# Windows Stage Manager 用户指南

## 首次启动

1. 解压版本化发布包并启动 `stage_manager.exe`。
2. 程序常驻系统托盘；默认 `dry_run=false`、`place_activated_window=true`、`affordance_preset=1`（平衡），前台切换时会把新活动窗口靠下居中，并自动整理被遮挡窗口。
3. 打开 `%LOCALAPPDATA%\WindowsStageManager\logs\manager.log`，可确认 `application_start` 中的实际 `dry_run` 值。

## 托盘参数设置

右键托盘图标并选择“打开可视化设置…”。左侧参数列表覆盖全部运行参数，右侧会显示中文含义、单位和合法范围；DIP 参数还会按设置窗口所在显示器的 DPI 显示近似像素长度，并在画布中实时标出绿色可点击边缘。可以连续调整多个参数后选择“保存并应用”，也可以一键恢复默认值。保存后程序会取消当前事务、重建内部窗口管理器、立即落盘并继续运行，不需要手动编辑配置文件，也不需要重启托盘程序。

“快速参数设置”子菜单继续提供常用预设和“自定义…”入口，适合只修改一个已熟悉的参数。菜单标题显示当前值，当前选项带勾选。

可调项目包括运行模式、新激活窗口靠下居中开关、可辨识度预设，以及顶部、左侧、右侧、底部各自的最小长度、最大长度、深度和动态比例。实际长度按“窗口对应边长 × 比例”计算，再限制在最小值和最大值之间；标准窗口的顶部深度优先采用系统实际标题栏高度，顶部回退高度只用于无边框或自绘标题栏。右侧和底部默认比顶部、左侧保留更大的区域。修改任一分边参数后预设会变为“自定义”。所有连续数值字段在常用值之后提供“自定义…”入口；最小长度超过当前最大长度时会同步抬高最大值，最大长度低于当前最小长度时会同步降低最小值。

参数仍持久化到 `%LOCALAPPDATA%\WindowsStageManager\settings.ini`，该文件用于下次启动恢复和故障诊断，正常操作无需打开。首次运行没有配置文件时使用以下关键默认值：

```ini
enabled=true
dry_run=false
place_activated_window=true
affordance_preset=1
top_depth_dip=32
left_depth_dip=40
right_depth_dip=64
bottom_depth_dip=64
max_managed_windows=20
max_consecutive_failures=3
```

程序开箱即可工作且不改变窗口尺寸。启用“新激活窗口靠下居中”时，普通窗口发生“非活动 -> 活动”切换后，其视觉框会水平居中并与工作区底部对齐；超高窗口改为让标题栏留在工作区顶部。关闭该开关时仍会修复被遮挡窗口，但不移动新活动窗口。同一窗口已经活动后，重复前台事件不会再次放置，用户手动拖动后的最终位置也不会被覆盖。

默认先尝试让每个受影响窗口在左上或右上保留互不重复的标题栏段和侧边段，并按已有通道负载交替，避免所有窗口堆到同一侧。判断组合时会扣除共享角部，顶部和侧边必须分别达到各自的动态长度和深度要求。只有顶部加侧边确实无解时才降级到单边，单边优先级依次为顶部、左侧、右侧、底部；同一可辨识等级内才比较归一化中心距离、移动窗口数和移动距离。

协调器会先完成所有纯位置求解；只有“顶部+侧边”和单边位置方案都明确无解时，才启用 Z-order fallback。fallback 采用 top-prefix（上层前缀保护）优先、bottom-first（同边界内最底层目标优先）的策略：先只允许最深层的局部顺序变化，若无解才逐层向上扩大可变化区间；在同一个插入边界内，从最底层目标窗口开始尝试。目标只会提升到当前求解所需的最深安全层级，不会默认提升到活动窗口正下方。日志中的 `activation_placement_used=true` 表示本批实际采用了靠下居中放置，`affordance_goal=top_and_side` 表示采用顶部加侧边，`affordance_goal_degraded=true` 表示降级到单边，`z_order_fallback_used=true` 表示使用了层级回退。

拖动结束和前台窗口切换都会触发布局检查。启用放置开关后，非活动窗口通过普通点击、任务栏或键盘切换成为前台时会靠下居中；如果该次激活同时进入拖动事务，则以用户拖动后的最终位置为准，不执行自动放置。若希望先观察计划或排查问题，从“快速参数设置 > 运行模式”选择“仅预览（DryRun）”；DryRun 会生成移动和重排计划，但不会调用 Win32 修改窗口。恢复实际管理时选择“应用窗口调整”。

## 日常操作与故障处理

- 托盘菜单可暂停/恢复管理、修改参数或退出程序。
- `Ctrl+Alt+F12` 会立即停用自动管理并取消当前移动事务。
- 连续 API 或快照故障达到阈值后，程序会自动停用；检查日志、从托盘切换到 DryRun 后再重新启用。
- 显示器、DPI、工作区变化以及队列溢出会触发全量快照重建。
- `Unsatisfiable` 表示激活居中、位置修复与安全 Z-order fallback 的组合没有完整满足硬约束的方案；程序不会应用部分计划、强行越界或让其他窗口跨越活动窗口。

## 回滚

发布目录中的 `current.json` 指向当前 ZIP，`rollback.json` 指向上一个已提升 ZIP。回滚时：退出程序，备份日志和配置，解压 `rollback.json` 指定的版本到新的目录；启动后立即从托盘切换到 DryRun 验证。不要覆盖正在运行的可执行文件。
